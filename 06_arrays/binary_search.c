#include <stdio.h>
#include <stdlib.h>

// to be completed later

size_t binarySearch(const int b[], int searchKey, size_t low, size_t high);

int main(void)
{

    
    return (0);
}


size_t binarySearch(const int b[], int searchKey, size_t low, size_t high)
{

    return ();
}